# En una ventana, ver los pods
kubectl get pods -n escalabilidad-lab -w

# En OTRA ventana PowerShell, ver recursos actualizándose
while ($true) { 
    Clear-Host
    Write-Host "=== PROBLEMA: Solo 3 pods, NO escala ===" -ForegroundColor Red
    kubectl get pods -n escalabilidad-lab -l app=web-app
    Write-Host ""
    kubectl top pods -n escalabilidad-lab --containers 2>$null
    Start-Sleep -Seconds 2
}