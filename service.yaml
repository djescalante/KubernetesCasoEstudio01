apiVersion: v1
kind: Service
metadata:
  name: app-web-service
  namespace: escalabilidad-lab
spec:
  selector:
    app: web-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80